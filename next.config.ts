import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // No direct support for allowedDevOrigins in this Next.js version
  // To allow access via IP address, consider using rewrites or proxy setup
};

export default nextConfig;
